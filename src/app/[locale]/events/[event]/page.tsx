const EventPage = ({
    params,
}: {
    params: {
        locale: 'en' | 'el'
        event: string
    }
}) => {
    return (
        <div>EventPage</div>
    );
};

export default EventPage;
