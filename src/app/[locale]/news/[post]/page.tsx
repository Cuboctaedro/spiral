const PostPage = ({
    params,
}: {
    params: {
        locale: 'en' | 'el'
        post: string
    }
}) => {
    return (
        <div>PostPage</div>
    );
};

export default PostPage;
